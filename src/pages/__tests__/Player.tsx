describe('<Player />', () => {
	it('does stuff', () => {
		expect(1).toEqual(1)
	})
	// it('renders', async () => {
	// 	renderWithProviders(<PlayerPage />)

	// 	expect(screen.getByText('Hello, World')).toBeInTheDocument()
	// })
})
